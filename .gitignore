# 依赖
node_modules/

# 本地敏感配置（含订阅 token，严禁上传）
settings.json

# 运行时数据与快照（部署后自动生成）
snapshot/
script/
*.synced.yaml

# 历史备份文件（snapshot/ 已承担快照职责）
1.yaml
1 - 副本.yaml

# 工具目录
.zcode/

# 日志
npm-debug.log*

# AI skills 安装产生的 junction 链接（skills.sh 生成，指回 .agents/skills）
.claude/
agent/
